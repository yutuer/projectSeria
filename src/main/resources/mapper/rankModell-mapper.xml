<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pureland.common.db.dao.mysql.mapper.ShopModelMapper">

    <select id="queryRankModelList" resultType="rankModel">
        SELECT  `name` as name,
        `type` as type,
        `exp` as exp,
        `icon` as icon,
        `reward_gold` as rewardGold,
        `reward_oil` as rewardOil
        FROM `rank_model`
        WHERE 1=1
    </select>

</mapper>



