<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pureland.common.db.dao.mysql.mapper.ShopModelMapper">

    <select id="queryShopModelList" resultType="shopModel">
        SELECT  `order_id` as orderId,
        `race_type` as raceType,
        `shop_type` as shopType,
        `shop_name` as shopName,
        `item_name` as itemName,
        `shop_order` as shopOrder,
        `shop_icon` as shopIcon,
        `base_id` as baseId,
        `item_order` as itemOrder
        FROM `shop_model`
        WHERE 1=1
    </select>

</mapper>



