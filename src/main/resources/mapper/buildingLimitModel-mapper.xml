<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pureland.common.db.dao.mysql.mapper.BuildingLimitModelMapper">

    <select id="queryBuildingLimitModelList" resultType="buildingLimitModel">
        SELECT  `id` as id,
			    `base_id` as baseId,
			    `building_base_id` as buildingBaseId,
			    `building_count` as buildingCount
		FROM `building_limit_model`
		WHERE 1=1
    </select>

</mapper>



